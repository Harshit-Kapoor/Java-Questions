package LinkedList;

public class FindMidOfLinkedList {

/*
    public ListNode middleNode(ListNode head) {

        ListNode slow = head;
        ListNode fast = head;

        while (fast.next != null && fast.next.next != null) {         // if anyone becomes false exit (fast.next for odd len and f.next.next for even len)
            slow = slow.next;                                         // move by 1
            fast = fast.next.next;                                    // move by 2
        }

        return slow;

    }
*/
}

